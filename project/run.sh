#!/bin/bash

# 1. ย้ายเข้าไปที่โฟลเดอร์โปรเจกต์ (แก้ path ให้ตรงกับเครื่องเพื่อนถ้าเขาไม่ได้ใช้ user 'pi')
cd /home/pi/project

# 2. ทำความสะอาดระบบ (Self-Healing)
echo "🧹 Cleaning up old processes..."
# ฆ่าโปรแกรม Python เก่าที่อาจจะค้างอยู่
sudo pkill -9 -f main.py
# ฆ่า Driver ของ DHT22 ที่ชอบค้าง (สำคัญมาก!)
sudo killall -9 libgpiod_pulsein

# รอแป๊บนึงให้ระบบเคลียร์ RAM
sleep 2

# 3. รันโปรแกรม
echo "🚀 Starting Smart Garden System..."
# เปิดใช้งาน Virtual Environment (ถ้ามี)
source venv/bin/activate

# รันไฟล์หลัก
python3 main.py